# requirements.txt
# Core scientific stack
numpy>=1.24.0
scipy>=1.10.0        # fallback LP solver (when Gurobi unavailable)

# Process mining
pm4py>=2.7.0         # XES/PNML I/O and Petri net representations
graphviz>=0.20.0     # Petri net PDF rendering

# Optimization (Gurobi)
# Install separately — requires license:
#   pip install gurobipy

gurobipy==12.0.2
# Analysis & reporting
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Development / testing
pytest>=7.0.0
